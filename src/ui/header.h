#pragma once

namespace ui {

// Draws the status header bar across the top of the main viewport.
// Returns the bar's height in pixels so other panels can be placed below it.
float DrawHeaderBar();

} // namespace ui
